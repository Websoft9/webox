# Websoft9 功能详细设计说明书 V1.1

**目录**

- [Websoft9 功能详细设计说明书 V1.1](#websoft9-功能详细设计说明书-v11)
  - [1. 引言](#1-引言)
  - [2. 管理员设置功能设计](#2-管理员设置功能设计)
  - [3. 管理员设置接口设计](#3-管理员设置接口设计)
  - [4. 管理员设置数据库设计](#4-管理员设置数据库设计)
  - [5. 附录](#5-附录)

## 1. 引言

本文档为 **Websoft9架构升级** 的详细设计文档，本文档的编写目的在于明确 **Websoft9架构升级** 需求的开发途径以及应用方法，通过此文档，为此 **Websoft9架构升级** 需求的维护提供清晰、详细的设计，为下阶段开发工作的开展起到指导作用。

本文档的预期读者是 **Websoft9架构升级** 需求相关的业务人员、开发人员以及系统运维人员。

## 2. 管理员设置功能设计

支持管理员对平台的默认时区、默认语言、推送通知、License进行统一设置。

- **功能描述**

1. 【系统时区设置】

支持管理员配置平台的默认时区：

- 【时区选择】从全球时区列表中选择默认时区。
- 【时区显示】支持UTC偏移量和时区名称显示。
- 【自动检测】支持根据服务器位置自动检测时区。
- 【用户覆盖】允许用户在个人设置中覆盖系统默认时区。

支持的时区包括：

- Asia/Shanghai (UTC+8)
- America/New_York (UTC-5/-4)
- Europe/London (UTC+0/+1)
- Asia/Tokyo (UTC+9)
- 其他标准时区

2. 【系统语言设置】

支持管理员配置平台的默认语言：

- 【语言选择】从支持的语言列表中选择默认语言。
- 【语言包管理】支持语言包的上传和更新。
- 【自动检测】支持根据浏览器语言自动检测。
- 【用户覆盖】允许用户在个人设置中覆盖系统默认语言。

支持的语言包括：

- 简体中文 (zh-CN)
- 繁体中文 (zh-TW)
- English (en-US)

## 3. 管理员设置接口设计

**获取系统所有配置**

```text
GET /api/v1/system-configs
```

查询参数：

| 参数    | 类型   | 必填 | 默认值 | 描述       |
| ------- | ------ | ---- | ------ | ---------- |
| keyword | string | 否   | -      | 搜索关键词 |

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 1,
        "config_key": "system_name",
        "config_value": "Websoft9平台",
        "config_type": "STRING",
        "category": "basic",
        "description": "系统名称",
        "is_readonly": false,
        "is_encrypted": false,
        "default_value": "Websoft9",
        "sort_order": 1
      },
      {
        "id": 2,
        "config_key": "smtp_enabled",
        "config_value": "true",
        "config_type": "BOOLEAN",
        "category": "smtp",
        "description": "是否启用SMTP",
        "is_readonly": false,
        "is_encrypted": false,
        "default_value": "false",
        "sort_order": 1
      }
    ]
  }
}
```

**更新系统配置**

```text
PUT /api/v1/system-configs
```

请求体参数：

| 参数名           | 数据类型 | 是否可空 | 描述     |
| ---------------- | -------- | -------- | -------- |
| config_key       | string   | 否       | 配置键   |
| config_value     | string   | 否       | 配置值   |
| config_type      | string   | 否       | 配置类型 |
| category         | string   | 否       | 配置分类 |
| description      | string   | 是       | 配置描述 |
| sort_order       | int      | 是       | 排序     |

请求示例：

```json
{
  "configs":[
      {
        "config_key": "system.name",
        "config_value": "Websoft9平台",
        "config_type": "STRING",
        "category": "basic",
        "description": "系统名称",
        "sort_order": 1
      },
      ...
    ]
}
```

**获取基础配置**

```text
GET /api/v1/system-configs/basic
```

查询参数：

| 参数    | 类型   | 必填 | 默认值 | 描述       |
| ------- | ------ | ---- | ------ | ---------- |
| keyword | string | 否   | -      | 搜索关键词 |

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 1,
        "config_key": "system_name",
        "config_value": "Websoft9平台",
        "config_type": "STRING",
        "category": "basic",
        "description": "系统名称",
        "is_readonly": false,
        "is_encrypted": false,
        "default_value": "Websoft9",
        "sort_order": 1
      },
      ...
    ]
  }
}
```

**获取安全配置**

```text
GET /api/v1/system-configs/security
```

查询参数：

| 参数    | 类型   | 必填 | 默认值 | 描述       |
| ------- | ------ | ---- | ------ | ---------- |
| keyword | string | 否   | -      | 搜索关键词 |

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 1,
        "config_key": "password_min_length",
        "config_value": "8",
        "config_type": "INTEGER",
        "category": "security",
        "description": "密码最小长度",
        "is_readonly": false,
        "is_encrypted": false,
        "default_value": "Websoft9",
        "sort_order": 1
      },
      ...
    ]
  }
}
```

**获取系统邮件配置**

```text
GET /api/v1/system-configs/email
```

查询参数：

| 参数    | 类型   | 必填 | 默认值 | 描述       |
| ------- | ------ | ---- | ------ | ---------- |
| keyword | string | 否   | -      | 搜索关键词 |

响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 1,
        "config_key": "smtp_host",
        "config_value": "127.0.0.1",
        "config_type": "STRING",
        "category": "email",
        "description": "SMTP服务器地址",
        "is_readonly": false,
        "is_encrypted": false,
        "default_value": "",
        "sort_order": 1
      },
      ...
    ]
  }
}
```

**测试SMTP配置**

```text
POST /api/v1/system-configs/smtp/test
```

请求体参数：

| 参数名     | 数据类型 | 是否可空 | 描述     |
| ---------- | -------- | -------- | -------- |
| test_email | string   | 否       | 测试邮箱 |

## 4. 管理员设置数据库设计

**系统配置表（system_configs）**

| 字段名        | 类型            | 约束               | 默认值                                        | 注释     |
| ------------- | --------------- | ------------------ | --------------------------------------------- | -------- |
| id            | BIGINT UNSIGNED | PK, AUTO_INCREMENT | -                                             | 配置ID   |
| config_key    | VARCHAR(64)     | NOT NULL, UNIQUE   | -                                             | 配置键   |
| config_value  | TEXT            | -                  | NULL                                          | 配置值   |
| config_type   | ENUM            | -                  | 'STRING'                                      | 配置类型 |
| category      | VARCHAR(32)     | NOT NULL           | -                                             | 配置分类 |
| description   | TEXT            | -                  | NULL                                          | 配置描述 |
| is_readonly   | TINYINT(1)      | -                  | 0                                             | 是否只读 |
| is_encrypted  | TINYINT(1)      | -                  | 0                                             | 是否加密 |
| default_value | TEXT            | -                  | NULL                                          | 默认值   |
| sort_order    | INT             | -                  | 0                                             | 排序     |
| created_at    | DATETIME        | NOT NULL           | CURRENT_TIMESTAMP                             | 创建时间 |
| updated_at    | DATETIME        | NOT NULL           | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

## 5. 附录

<!-- 这里写功能的所包含的枚举值、字典等定义，或者相关的参考文档引用 -->
